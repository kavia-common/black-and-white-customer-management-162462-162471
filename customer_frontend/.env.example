# Public env variables for Create React App must be prefixed with REACT_APP_
# Base URL for API calls. If omitted, dev server proxy (see package.json) will be used.
# Example for direct calls:
# REACT_APP_API_BASE=http://localhost:3001
REACT_APP_API_BASE=
